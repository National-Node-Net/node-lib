name: Python Vuln Scan

on:
  pull_request:
  workflow_dispatch:

jobs:
  scan_pr:
    uses: NationalDigitalTwin/shared-workflows/.github/workflows/python-generate-and-scan-application.yml@main
    with:
      SCAN_NAME: ${{ github.run_number }}
