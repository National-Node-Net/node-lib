name: Create release PR

on:
  push:
    branches:
      - "main"

permissions:
  contents: write
  pull-requests: write

jobs:
  create-release-pr:
    uses: NationalDigitalTwin/shared-workflows/.github/workflows/python-create-release-pr.yml@main
