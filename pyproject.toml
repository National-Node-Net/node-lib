[build-system]
requires = ["setuptools>=61.2.0", "wheel==0.46.3", "pip-tools==7.5.2"]
build-backend = "setuptools.build_meta"

[project]
name = "node-lib"
version = "3.1.1"
authors = [{name = "National Digital Twin Programme"}]
description = "A helper package for building Adapters, Mappers and Projectors for Integration Architecture Node"
requires-python = ">=3.9"
license = {file = "LICENSE"}
readme = "README.md"
dependencies = [
    "colored==2.3.1",
    "rdflib==7.5.0",
    "Werkzeug==3.1.5",
    "PyJWT==2.10.1",
    "requests==2.32.5",
    "pytz==2025.2",
    "confluent-kafka==2.13.0",
    "opentelemetry-api==1.39.1",
    "opentelemetry-sdk==1.39.1"
]
classifiers = [
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3 :: Only",
    "Topic :: Software Development",
    "Typing :: Typed",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX",
    "Operating System :: Unix",
    "Operating System :: MacOS",
]

[project.optional-dependencies]
dev = [
    "pre-commit==4.3.0",
    "ruff==0.9.4",
    "types-pytz==2025.2.0.20251108",
    "urllib3==2.6.3",
    "types-requests==2.32.4.20260107",
    "mypy==1.19.1",
    "mkdocs==1.6.1",
    "cyclonedx-bom==7.2.1"
]

[tool.setuptools.dynamic]
readme = {file = ["README.md"]}

[tool.ruff]
target-version = "py39"
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "C",  # flake8-comprehensions
    "B",  # flake8-bugbear
    "UP", # pyupgrade
]
ignore = [
    "C901", # too complex
]
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "venv",
]
line-length = 120

[project.urls]
Repository = "https://github.com/National-Digital-Twin/node-lib"

[tool.distutils.bdist_wheel]
universal = true

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
include = ["ia_map_lib*"]
namespaces = true

[tool.setuptools.package-data]
"ia_map_lib" = ["sbom.json"]

[tool.mypy]
no_implicit_optional = false
ignore_missing_imports = true
modules = ["ia_map_lib", "tests", "debug"]
